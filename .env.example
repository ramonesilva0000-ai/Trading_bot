# Copy to .env and fill in. Never commit .env.
EXCHANGE_API_KEY=
EXCHANGE_API_SECRET=
# Optional (OKX, KuCoin, Bitget, etc.)
EXCHANGE_API_PASSPHRASE=
# Required for the phone API when binding to anything other than 127.0.0.1.
# Generate with: openssl rand -hex 24
API_KEY=
